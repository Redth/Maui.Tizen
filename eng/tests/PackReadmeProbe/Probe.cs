namespace Maui.Tizen.Internal.PackReadmeProbe;

/// <summary>
/// Minimal content so the probe produces a valid package. The README in that package is
/// what the regression actually asserts on.
/// </summary>
internal static class Probe
{
	internal const string Marker = "pack-readme-probe";
}
