<?xml version="1.0" encoding="utf-8"?>
<!--
  api-version is 11, matching $(TizenManifestApiVersion) in Directory.Build.props and
  eng/baselines.json > target.tizenManifestApiVersion (TizenFX API15 / Samsung.Tizen.Ref.API15).

  dotnet/maui still emits api-version 7 for its Tizen heads; do not copy that value back in.

  The icon elements are left as maui-appicon-placeholder so GenerateTizenManifest resolves them
  to the icon names the Resizetizer actually produced (appicon.xhigh.png / appicon.high.png).
  The package id, version and label are authored explicitly here and are therefore preserved:
  the generator only fills placeholders and empty values, it never overwrites a real one.
-->
<manifest package="org.tizen.example.MauiTizenSample" version="1.0.0" api-version="11" xmlns="http://tizen.org/ns/packages">
  <profile name="common" />
  <ui-application appid="org.tizen.example.MauiTizenSample"
                  exec="Maui.Tizen.Sample.dll"
                  type="dotnet"
                  multiple="false"
                  taskmanage="true"
                  launch_mode="single"
                  hw-acceleration="on">
    <label>Maui.Tizen Sample</label>
    <icon>maui-appicon-placeholder</icon>
    <icon dpi="xhdpi">maui-appicon-placeholder</icon>
    <icon dpi="hdpi">maui-appicon-placeholder</icon>
    <metadata key="http://tizen.org/metadata/prefer_dotnet_aot" value="true" />
  </ui-application>
  <shortcut-list />
  <privileges>
    <privilege>http://tizen.org/privilege/internet</privilege>
  </privileges>
  <dependencies />
  <provides-appdefined-privileges />
</manifest>
